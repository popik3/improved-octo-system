import pandas as pd

data = {
    "Имя": ["Анна", "Михаил", "Алиса"],
    "Возраст": [29, 35, 1],
    "Город": ["Москва", "Санкт-Петербург", "Новосибирск"],
}

df = pd.DataFrame(data,index=["a","b","c"])
#print(df)
#print( df[["Имя", "Возраст"]])
# Выборка строки по метке
"""
print(df.loc["a"])
print("\n")

# Выборка строки по позиции
print(df.iloc[1])
"""
print(df[df["Город"] == "Москва"])
print(df[df["Возраст"] > 25])
print(df[df["Возраст"] > 25]["Имя"])