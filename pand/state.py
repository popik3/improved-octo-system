import pandas as pd

Data = {
    '1' :[1,6,11],
    '2' :[3,8,13],
    '3' :[4,9,14],
    '4' :[5,10,15],
}
df = pd.DataFrame(Data, index=["a","b","c"])
print(df)
print(df.sum())
print(df.sum().sum())
print(df.describe())
"""
count — количество непустых значений

mean — среднее значение

std — стандартное отклонение

min — минимальное значение

25%, 50%, 75% — квартили (значения на 25%, 50% и 75% упорядоченных данных)

max — максимальное значение
"""